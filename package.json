{
  "name": "opticai",
  "version": "1.0.0",
  "description": "OpticAI - Restaurant Intelligence",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required for static HTML'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.3.1"
  }
}
